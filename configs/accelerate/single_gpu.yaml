# configs/accelerate/single_gpu.yaml
# One process, no DeepSpeed — for smoke tests / single-GPU debug runs.
#   accelerate launch --config_file configs/accelerate/single_gpu.yaml ...
compute_environment: LOCAL_MACHINE
distributed_type: "NO"
mixed_precision: bf16
num_machines: 1
num_processes: 1
use_cpu: false
