# CAAD runtime dependencies.
# `pip install -e .` (pyproject.toml) is the packaged path; this file mirrors it
# for `pip install -r requirements.txt`. Keep the two in sync.

# --- core training ---
torch>=2.4
transformers>=4.49            # Qwen2.5-VL support
accelerate>=0.34
peft>=0.13
deepspeed>=0.16

# --- video VLM I/O ---
qwen-vl-utils
decord                        # video-decode backend used by qwen-vl-utils
opencv-python-headless>=4.9   # corruption engine (data/corruption.py)
numpy>=1.26
pyyaml>=6.0

# --- rollout + eval inference ---
vllm>=0.8

# --- logging / viz ---
wandb
matplotlib
